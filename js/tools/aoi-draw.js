// AOI drawing tool. This module would provide functionality for
// drawing a polygon on the map which defines the area of interest.
// The implementation here is currently a stub. Use mapbox-gl-draw or
// custom handlers to enable drawing in the future.

/**
 * Start AOI drawing on the provided map. At present this function
 * simply logs a message. In a full implementation it would allow
 * users to click on the map to define a polygon and update the
 * application state with the coordinates.
 *
 * @param {maplibregl.Map} map The map instance.
 * @param {Object} state The application state.
 */
export function startAoiDrawing(map, state) {
    console.debug('AOI drawing is not yet implemented');
}