// Closures tool. Manages the application of road closures within the
// area of interest. This is a stub implementation and does not
// actually modify any data. Extend this module to keep track of
// closed links and feed that information into the computation.

export function toggleClosure(linkId, state) {
    console.debug(`Toggle closure for link ${linkId} (not implemented)`);
}